#include <bits/stdc++.h>

using namespace std;

class UninonFind {
	public:
		vector <int> pr, sz();
		UnionFind(n): par(n), sz(n, 1){
			for (int i = 0; i < n; i++)
				par[i] = i;
		}

		
}

int main(){
	int n, m;
	cin >> n >> m;
	for (int)
	return 0;
}
