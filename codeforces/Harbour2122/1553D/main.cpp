#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	int tc;
	cin >> tc;
	cin.ignore();
	while (tc--){
		string s, t;
		cin >> s;
		cin >> t;
		cout << s << "\n";
		cout << t << "\n";
	}

	return 0;
}
