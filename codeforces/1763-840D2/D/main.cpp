/*
 *    author: Tien M. Pham
 *    created: 20.12.2022
 * */

#include <bits/stdc++.h>

using namespace std;

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  // Input data
  int8_t t;
  cin >> t;
  while (t--){
	  int8_t n, i, j, x, y;
	  cin >> n >> i >> j >> x >> y;
	  cout << n << " " << i << " " << j << " " << x << " " << y << "\n";
  }

  // Output result

  return 0;
}
