/*
    Author: Tien M. Pham
    Created: 03.10.2023
*/

#include <bits/stdc++.h>

using namespace std;

class sqrtDecomp {
privite:
    uint16_t len;
    vector<uint16_t> b;

public:
    void sqrtDecomp() {
        this->len = 0;
    }

    void sqrtDecomp(vector<uint16_t> &a) {
        this->len = 
    }

    void count_distinct
};

int32_t main() {
    uint16_t n;
    cin >> n;
    
    vector<uint16_t> a(n);
    for (uint16_t &x : a)
        cin >> x;

    // TODO: Initial SQRT Tree

    uint32_t q;
    cin >> q;
    while (q--) {
        uint16_t l, r;
        cin >> l >> r;
        // TODO: Call update func to query l and r

    }

    return 0;
}
